<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lenicliu.spring.boot.mybatis.mapper.LogMapper">
	<select id="list" resultType="Log" parameterType="java.lang.Long">
		SELECT * FROM TB_USER_LOG WHERE UID = #{uid}
	</select>
</mapper>
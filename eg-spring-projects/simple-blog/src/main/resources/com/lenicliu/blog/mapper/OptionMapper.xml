<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lenicliu.blog.mapper.OptionMapper">
    <select id="find" resultType="Option" parameterType="java.lang.String">
        SELECT * FROM OPTION WHERE NAME = #{name}
    </select>
</mapper>